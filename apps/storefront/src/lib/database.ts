/**
 * Supabase / Postgres types for Jeyjo core tables (customers, web_profiles, search_events, audit_log).
 * Regenerate: `pnpm db:types` from repo root (requires local Supabase running).
 */
export type { Database, Json } from '@jeyjo/database-types'
